"use client";

import React from "react";
import { Users, LogOut, Bell, Shield } from "lucide-react";
import Link from "next/link";

export default function CandidateLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}